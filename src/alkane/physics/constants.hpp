#pragma once

namespace physics {
inline constexpr float gravity = -980.0f;
inline constexpr float friction_coefficient = 4.0f; // I know this shouldn't be hardcoded but just doing this for rn
} // namespace physics
